name: zz-diagnose-fetch
on:
  push:
    branches: [fix/torch-cpu-and-release-v2.0.2]
jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.2.2
        with: { lfs: true, fetch-depth: 0, ref: fix/torch-cpu-and-release-v2.0.2 }
      - uses: conda-incubator/setup-miniconda@v3
        with: { auto-update-conda: true, python-version: "3.12", auto-activate-base: false }
      - name: Clone repo
        run: |
          rm -rf *
          git clone https://github.com/miquelduranfrigola/eos6m2k.git
      - name: Install ersilia
        run: |
          conda run -n base git-lfs install
          conda run -n base python -m pip install "git+https://github.com/ersilia-os/ersilia.git"
      - name: Fetch + show env-creation detail
        env: { MODEL_ID: eos6m2k, PYTHONUNBUFFERED: "1" }
        run: |
          conda run -n base bash -c "ersilia -v fetch $MODEL_ID --from_dir $MODEL_ID" > /tmp/fetch.log 2>&1 || true
          echo "================ conda envs ================"; conda env list
          echo "================ env creation / conda create / error lines in fetch.log ================"
          grep -niE "conda create|conda env|creating.*env|environment|envs/eos6m2k|venv|virtual|No such file|error|fail|exit code|not found|eos6m2k.*python" /tmp/fetch.log | grep -viE "DeprecationWarning" | tail -40
          echo "================ where did deps install? (torch/xgboost) ================"
          grep -niE "Successfully installed torch|Successfully installed xgboost|Installing collected packages.*torch|conda run -n|Run commandlines on|environment name is" /tmp/fetch.log | tail -15
